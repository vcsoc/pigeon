const test=require('node:test'),assert=require('node:assert/strict'),{Worker}=require('node:worker_threads'),path=require('node:path');
test('folder tree worker bounds huge trees and preserves counts',async()=>{const assets=Array.from({length:1200},(_,index)=>({locationId:'root',path:`C:/root/folder-${index}/child/file.jpg`}));const result=await new Promise((resolve,reject)=>{const worker=new Worker(path.join(__dirname,'..','electron','folder-tree-worker.js'),{workerData:{locations:[{id:'root',path:'C:/root'}],assets,limits:{root:300},collapsedKeys:[]}});worker.once('message',resolve);worker.once('error',reject);});assert.equal(result[0].folders.length,300);assert.equal(result[0].visibleFolders,2400);assert.equal(result[0].totalFolders,2400);assert.equal(result[0].folders[0].count,1);});
