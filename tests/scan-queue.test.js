const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),os=require('node:os'),path=require('node:path');
test('large scan queues remain compact when stored separately from checkpoints',()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),'pigeon-queue-')),queue=Array.from({length:100000},(_,i)=>`D:/large/folder-${i}/file-${i}.jpg`),queueFile=path.join(root,'queue.json');fs.writeFileSync(queueFile,JSON.stringify(queue));const checkpoint=JSON.stringify({root:'D:/large',nextIndex:50000,discovered:queue.length,complete:true,startedAt:Date.now()});assert.ok(checkpoint.length<200);assert.equal(JSON.parse(fs.readFileSync(queueFile,'utf8')).length,100000);fs.rmSync(root,{recursive:true,force:true});});
