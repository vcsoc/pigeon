const test=require('node:test'),assert=require('node:assert/strict'),{unchangedWatchFile}=require('../electron/scan-watch-events');
test('unchanged file notifications do not request another root scan',()=>{const asset={path:'/root/a.jpg',modified:1234,size:567};assert.equal(unchangedWatchFile(asset,'/root/./a.jpg',{mtimeMs:1234,size:567}),true);for(const stat of [undefined,{mtimeMs:1235,size:567},{mtimeMs:1234,size:568},{mtimeMs:NaN,size:567}])assert.equal(unchangedWatchFile(asset,asset.path,stat),false);assert.equal(unchangedWatchFile(asset,'/root/b.jpg',{mtimeMs:1234,size:567}),false);for(const flag of ['sourceMissing','sourcePending','permissionDenied'])assert.equal(unchangedWatchFile({...asset,[flag]:true},asset.path,{mtimeMs:1234,size:567}),false);assert.equal(unchangedWatchFile(null,asset.path,{mtimeMs:1234,size:567}),false);});
