const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');const source=fs.readFileSync(require('node:path').join(__dirname,'../electron/main.js'),'utf8');const code=source.slice(source.indexOf('function publicLibrarySummary()'),source.indexOf('function passwordKey'));
for(const saved of [undefined,true,false])test(`machine GPU setting overrides portfolio setting ${saved}`,()=>{const context={library:{assets:[],collections:[],settings:{preferences:{hardwareAcceleration:saved,theme:'dark'}}},runtimePreferences:{hardwareAcceleration:false},visibleLocations:x=>x,publicFolderLocks:()=>({}),publicCollections:()=>[],portfolios:[],activePortfolioId:'p'};vm.createContext(context);vm.runInContext(code,context);const preferences=context.publicLibrarySummary().settings.preferences;assert.equal(preferences.hardwareAcceleration,false);assert.equal(preferences.theme,'dark');});
