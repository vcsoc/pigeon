# Pigeon 0.2.93

- Undo/Redo icons sit immediately before Save edits, separated from controls on both sides. Remove the oversized tool-panel undo button. Unsaved layers, crops, names, transformations and adjustments have bounded, session-local history (80 states / 8 MiB); pointer gestures coalesce. Ctrl+Z / Ctrl+Shift+Z work outside text inputs. Accepted AI pixels are a new base, not reversible history.
- Editor checkerboard starts enabled and its toolbar toggle persists. Preferences clearly distinguish viewer and editor grids.
- Previously flattened JPEG previews of local transparent images repair asynchronously when visible, without delaying cached display or triggering a library scan. Requests are bounded and serialized; locks, source version and portfolio ownership are rechecked. Offline/network sources retain cached previews and can be explicitly refreshed with F5 when available.
- Duplicates are written beside the source as `<name>-DDHHmmss.<extension>` (with an extra numeric suffix on same-second collisions). Existing location ownership is retained; duplication never adds a Folders-panel location or creates a physical folder. Copies retain prior memberships and are added to a reused/new Duplicates collection; protected collections are respected.
- Duplication updates stream indexes and virtual geometry immediately, retaining source cards, selection, sorting and scroll anchors. Native checks cover 350 and 3000 images without triggering a scroll event.
- Fullscreen return restores scroll synchronously before layout refresh; hidden-viewer scroll events no longer reset the virtual window. Native 1000-image check retained the same selected card at 6000px: first gallery paint 89ms, full return 243ms. This is fixture evidence, not a 40k/physical-compositor latency certification.

631 unit/component tests pass. Native history, cached-alpha repair, layer controls, gallery clicks, duplication and fullscreen return checks pass. Earlier large-library, network and platform validation gaps remain open.
