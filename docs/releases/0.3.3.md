# Pigeon 0.3.3

- Replace the old busy/loading GIF on startup, portfolio opening and folder opening with the supplied `pigeon-busy-transparent.webm`.
- Preserve the VP9 video stream and alpha channel without re-encoding. Remove the audio track; all video elements are also muted, looping and inline by default.
- Remove rectangular video shadows/backgrounds and keep the clip itself transparent.
- Pause hidden animations and resume only visible loading surfaces. Observe only video/ancestor visibility attributes, not gallery subtree mutations.

Validation: native Electron playback decoded 1280×720 frames with alpha spanning 0–255; muted/zero volume, transparent CSS background, no box shadow, and hidden playback pause verified. ffprobe confirms a single VP9 stream with alpha metadata and no audio. 657 unit tests pass.
