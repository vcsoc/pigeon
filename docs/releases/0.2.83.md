# Pigeon 0.2.83 — close context menus when dragging

Starting a Smart Folder, collection or physical-folder drag immediately hides the context menu and its order/action submenus, before measuring or showing the drop preview. Drag destination and guarded move behavior are unchanged.

Validation: 539 component tests pass. Isolated Electron checks open the real context menu with right-click, dispatch a sidebar drag and verify all context menus are hidden. Collection, Smart Folder and physical-folder cases pass, including the existing placement/root/gutter checks. Drag events in this harness are programmatic, not native desktop pointer gestures. The supplied screen recording was inspected. GitNexus does not yet index the recently added drag-preview files, so impact remains UNKNOWN rather than low; the exact capture-phase handler and existing menu-close helper were reviewed directly and covered by the checks above.

This remains a prerelease: previously disclosed large-library startup, platform runtime and macOS signing gaps are unchanged. Binary uploads may exceed the user's 15-second command limit; draft creation is not publication.
